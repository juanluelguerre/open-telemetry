global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "helloopentelemetry"
    metrics_path: "/metrics"
    static_configs:
      # - targets: ["helloopentelemetry:8080"]
      - targets: ["otel-collector:9091"] # Port to recollect metrics from the otel-collector
